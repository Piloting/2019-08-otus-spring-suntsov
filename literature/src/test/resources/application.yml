spring:
  shell:
    interactive:
      enabled: false  
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db
    username: sa
    password:
    data: test-data.sql
    sql-script-encoding: UTF-8
