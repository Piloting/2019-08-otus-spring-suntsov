spring:
  shell:
    interactive:
      enabled: false  
  messages:
    basename: localization/bundle
question:
  #путь до CSV-файла
  baseFileName: questions.csv
  #текущая локаль
  locale: ru

# процент правильных ответов для зачёта
successPercent: 50

application.version: 1.01